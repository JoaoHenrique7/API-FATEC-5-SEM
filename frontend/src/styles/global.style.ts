import { StyleSheet } from 'react-native';
import { CurrentTheme } from '../contexts/ThemeContext/ThemeContext.context';

const globalStyle = (theme: CurrentTheme) =>
	StyleSheet.create({});

export default globalStyle;
